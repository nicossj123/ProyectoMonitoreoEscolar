import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Monitoreo_escolar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
